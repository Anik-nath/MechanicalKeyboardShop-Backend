import { Router } from 'express';

export const router = Router();

// const allRoutes = [
//   {
//     path: '',
//     route: ,
//   },
// ];
// allRoutes.map((ele) => router.use(ele.path, ele.route));
